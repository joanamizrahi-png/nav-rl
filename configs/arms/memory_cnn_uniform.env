# memory_cnn with NO distance curriculum: goals drawn uniformly from 2-10 m from
# step one (2026-09-25, Joana: "would an arm without a distance curriculum work
# better?"). The control for the ramp: easy goals are always in the batch (the
# bootstrap the fixed-6 m arms never had), hard goals are always in the batch, and
# the task never changes -- so the critic is never shocked by a band step.
# Same coverage cutoff as the slow-curriculum arms so the comparison is ramp vs no ramp.
ENCODER=nature
IMGFIX=0
BOXMEM=5
BOXMEMAGG=mean
COLLAHEAD=0.7
GOALRANGE=2,10
GOALDIST_START=              # empty = no curriculum; the range above IS the task
# Coverage rule (2026-09-25, Joana): terminate below 0.4, graded cost below 0.7.
# COH raised 10 -> 50 so the cost is felt: 50 x (0.7 - cov) x 0.01 = 0.10/step at
# cov 0.5, 0.15 at 0.4. At the base weight of 10 it was 0.02/step -- symbolic.
COHTERM=0.4
COHTAU=0.7
COH=50
# With no curriculum nothing calls set_goal_dist, which is where the step budget
# (40 + 16/m x band top) is applied -- so it must be set by hand for 10 m goals.
MAXSTEPS=200
GPUS=4
