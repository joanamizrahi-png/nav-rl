# Runbook — Sunday 2026-08-16 (policy launch day)

Every command for the morning, in order. Rule: after every sbatch, verify
with sacct + log tail BEFORE fetching or building on it.

## 0. Overnight inventory (first coffee)

    squeue -u jmizrahi
    sacct -S 2026-08-15-16:00 --format=JobID,JobName%14,State,Elapsed | grep -v -E "batch|extern" | tail -20

Expect: 8x cache-gen COMPLETED, train-se (v13 smoke) COMPLETED,
inf-sem (360 spin) COMPLETED. Any TIMEOUT/FAILED cache job: just relaunch the
same SWEEPS range (jobs skip already-rendered sweeps):

    cd /scratch/m000204-pm06b/joana/nav-rl
    sbatch --time=06:00:00 --export=SCENE=rugd_trail_00,SWEEPS=<the-failed-range> scripts/slurm/cache_gen.sh

## 1. Policy smoke gate (10k steps, ~30-60 min)

    cd /scratch/m000204-pm06b/joana/nav-rl && git pull
    sbatch --mem=96G --export=CACHE=1,SMOKE=1 scripts/slurm/train_ppo_real.sh
    squeue -u jmizrahi

When it leaves the queue:

    sacct -S 2026-08-16 --name=ppo-real-smoke --format=JobID,State,Elapsed,ExitCode | grep -v -E "batch|extern"
    tail -5 /scratch/m000204-pm06b/joana/slurm-ppo-real-<JOBID>.out

Fetch the smoke rollout (Mac):

    cd "/Users/joanamizrahi/Documents/Education/Stanford/Quarter 3/CS399 Research/World Model/inference_runs/rl_rollouts"
    scp 'jmizrahi@login.marlowe.stanford.edu:/scratch/m000204-pm06b/joana/outputs/ppo_v14diff_trail00_SMOKE/rollout.mp4' smoke_v14diff.mp4

GATE (watch smoke_v14diff.mp4): observations look like the cached diffused
views (clean, not raster-holey) - vision doesn't stutter wildly as the robot
turns - HUD "cache=" numbers hover near 12cm/7deg, no huge spikes - robot
does robot things (moves, turns; competence NOT required at 10k steps).

## 2. Full policy launch (only after gate 1 passes)

    cd /scratch/m000204-pm06b/joana/nav-rl
    sbatch --mem=96G --export=CACHE=1 scripts/slurm/train_ppo_real.sh
    squeue -u jmizrahi

800k steps. This is the advisor's headline ask, launched.

## 3. v13 (analog bits) grading — render + score

    cd /scratch/m000204-pm06b/joana/NeoVerse && git pull
    sbatch --export=RUN_NAME=train_semantic_v13smoke_bits,NUM_CLASSES=14,CLIP=rugd_trail-6_01,BITS=1 scripts/slurm/inference_v8_run.sh

Verify (sacct + tail), then fetch (Mac):

    cd "/Users/joanamizrahi/Documents/Education/Stanford/Quarter 3/CS399 Research/World Model/inference_runs"
    scp -r 'jmizrahi@login.marlowe.stanford.edu:/scratch/m000204-pm06b/joana/inference_train_semantic_v13smoke_bits_rugd_trail-6_01_static_bits' .
    mv inference_train_semantic_v13smoke_bits_rugd_trail-6_01_static_bits v13smoke_rugd_trail-6_01_static_bits

Then say "landed" — scoring vs the 5-epoch references (v9@5 61.9 / smokes
66-69) is automated on this side.

## 4. Report cards over all scenes (background, GPU gaps)

    cd /scratch/m000204-pm06b/joana/nav-rl
    for S in rugd_trail_00 rugd_park-1_00 rugd_park-2_00 rugd_trail-4_00; do
    sbatch --export=SCENE=$S scripts/slurm/report_card.sh
    done

(Extend to every clip once these four look sane.)

## 5. 360 spin fetch (whenever, evidence material)

    sacct -j 430471 --format=JobID,State,Elapsed | grep -v -E "batch|extern"

If COMPLETED (Mac):

    cd "/Users/joanamizrahi/Documents/Education/Stanford/Quarter 3/CS399 Research/World Model/inference_runs"
    scp -r 'jmizrahi@login.marlowe.stanford.edu:/scratch/m000204-pm06b/joana/inference_train_semantic_v10_rugd_trail-6_01_pan_left_a360_head' .
    for d in inference_train_semantic_*; do mv "$d" "${d/inference_train_semantic_/}"; done 2>/dev/null

Then say "landed" — the spin grid gets stitched and you judge the full
hallucination loop.
